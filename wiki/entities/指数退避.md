---
type: entity
category: 架构模式
---

# 指数退避

轮询或重试时，每次失败后将等待间隔翻倍（或按指数增长），直到命中上限。换取空轮询的资源节省，代价是最坏情况延迟增大。

## 在各 repo 中的体现

- [[openclaw/dimensions/openclaw-performance-tradeoffs]] — 长时间无输出的命令轮询：`5s → 10s → 30s → 60s`，有新输出时立即重置为 5s
