---
watchlist: true
type: entity
category: 架构模式
---

# 故障隔离

将系统切分为独立单元，使单个单元的故障不蔓延至其他单元。常见手段：独立进程、独立包、依赖不聚合。

## 在各 repo 中的体现

- [[openclaw/dimensions/openclaw-dependency-strategy]] — 每个 channel extension 独立声明 SDK 依赖，不在 root package 聚合，核心运行时不因任何单个 channel SDK 变动受影响
