---
watchlist: true
type: entity
category: 架构模式
---

# 分层架构

将系统按职责切分为若干层次（如用户界面层、编排层、安全层、基础设施层），层间通过定义好的接口通信，下层不感知上层，变更局部化。

## 在各 repo 中的体现

- [[hermes-agent/dimensions/hermes-agent-architecture]] — 六层：用户界面层 → 编排层 → 安全层 → 插件层 → 工具层 → 基础设施层 → 可观测性层；数据流单向为主
