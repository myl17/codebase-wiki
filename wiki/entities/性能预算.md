---
type: entity
category: 架构模式
---

# 性能预算

将性能指标（启动时间、包体积、内存等）纳入 CI 检查，超出预置基线即构建失败，防止性能退化悄无声息地累积。

## 在各 repo 中的体现

- [[openclaw/dimensions/openclaw-testing-philosophy]] — `test:startup:bench:check` 对比 `cli-startup-bench.json` 基线 fixture，超出 budget 即 CI 失败；`test:perf:budget` 监控整体性能指标
