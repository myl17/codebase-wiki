---
type: entity
category: 架构模式
---

# 优雅降级

可选依赖缺失或外部服务不可用时，系统自动降级到功能受限但可运行的状态，而非直接崩溃。

## 在各 repo 中的体现

- [[hermes-agent/dimensions/hermes-agent-dependency-strategy]] — 20+ 可选 extras（memory providers、voice、browser 等）缺失时运行时自动降级，核心 7 个必须依赖之外均可选
